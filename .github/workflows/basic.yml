name: basic workflow

on:
  workflow_dispatch:

jobs:
  workflow:
    runs-on: ubuntu-latest
    
    steps:
      - name: display message
        shell: bash
        run: |
          echo "Hello world!"
